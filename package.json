{
  "name": "github-workflow",
  "version": "v1.0.4-230310",
  "license": "MIT",
  "description": "Repositório para testes de github workflow e scripts de automação",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
